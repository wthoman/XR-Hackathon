---
sidebar_position: 4
description: 'Send and receive custom networked events between connected Spectacles users using the Sync Kit event system for multiplayer logic.'
---

import CodeExamples from '@site/src/components/CodeExample';

import simpleSendReceiveTs from './code-examples/networked-events/ts/simple-send-receive.ts?raw';
import sendEventTs from './code-examples/networked-events/ts/send-event.ts?raw';
import messageInfoTs from './code-examples/networked-events/ts/message-info.ts?raw';
import eventDataTs from './code-examples/networked-events/ts/event-data.ts?raw';
import eventDataObjectTs from './code-examples/networked-events/ts/event-data-object.ts?raw';
import remoteOnlyTs from './code-examples/networked-events/ts/remote-only.ts?raw';

import simpleSendReceiveJs from './code-examples/networked-events/js/simple-send-receive.js?raw';
import sendEventJs from './code-examples/networked-events/js/send-event.js?raw';
import messageInfoJs from './code-examples/networked-events/js/message-info.js?raw';
import eventDataJs from './code-examples/networked-events/js/event-data.js?raw';
import eventDataObjectJs from './code-examples/networked-events/js/event-data-object.js?raw';
import remoteOnlyJs from './code-examples/networked-events/js/remote-only.js?raw';

# Networked Events

Networked events can be added to [SyncEntities](./sync-entity) for the purpose of sending one-off, real-time events or messages that do not change the state of the SyncEntity. For example, a networked event can be sent to play a sound effect or trigger a particle effect for all users.

Networked events are useful for triggering behaviors that take effect immediately and do not involve data that needs to be referenced again like a [storage property](./storage-properties.mdx). In other words, you can think of networked events as calling a function on all instances of a SyncEntity across the network.

### Basic Sending and Receiving

Before a SyncEntity sends an event, tell the SyncEntity to listen for the event using `SyncEntity.onEventReceived`. In the add method, pass the name of the event (for example, "sayHi") and the callback function that should run when the event is received. This can be done before the SyncEntity [setup](./sync-entity#syncentity-setup) is finished.

<CodeExamples
  typescriptCode={simpleSendReceiveTs}
  javascriptCode={simpleSendReceiveJs}
  lineRange={{ typescript: '9-13', javascript: '5-9' }}
/>

To send an event, use `SyncEntity.sendEvent()`, passing the name of the event as a parameter.

<CodeExamples
  typescriptCode={sendEventTs}
  javascriptCode={sendEventJs}
  lineRange={{ typescript: '9-9', javascript: '5-5' }}
/>

### MessageInfo

If you need more information about the event, like who it was sent by, the `SyncEntity.onEventReceived` callback supplies a MessageInfo object that provides info about the event message.

- `MessageInfo.senderUserId`: The [user ID](./user-information#userid) of the sender.
- `MessageInfo.senderConnectionId`: The [connection ID](./user-information#connectionid) of the sender.
- `MessageInfo.message`: The name of the event.
- `MessageInfo.data`: The data sent with the event.

<CodeExamples
  typescriptCode={messageInfoTs}
  javascriptCode={messageInfoJs}
  lineRange={{ typescript: '10-18', javascript: '5-10' }}
/>

### Including Event data

It is often useful to include data along with an event, just like passing information into a function using parameters.
`SyncEntity.sendEvent()` has an optional second parameter for event data that gets included in the MessageInfo object as `MessageInfo.data`.
Note: Networked events have a payload size limit of 100 KB.

<CodeExamples
  typescriptCode={eventDataTs}
  javascriptCode={eventDataJs}
  lineRange={{ typescript: '9-13', javascript: '5-9' }}
/>

Event data supports the following data types:

- string
- vec2
- vec3
- vec4
- quat
- object, as long as it is JSON serializable (example below)

<CodeExamples
  typescriptCode={eventDataObjectTs}
  javascriptCode={eventDataObjectJs}
  lineRange={{ typescript: '10-28', javascript: '5-23' }}
/>

### Event Recipients

Networked events are sent to all users in the session by default. To send a networked event to remote users only, pass `true` as a third parameter to `SyncEntity.sendEvent()`.

<CodeExamples
  typescriptCode={remoteOnlyTs}
  javascriptCode={remoteOnlyJs}
  lineRange={{ typescript: '9-9', javascript: '5-5' }}
/>

### Payload and Rate Limits

Networked events count against [payload and message rate limits](./payload-and-rate-limits). Each networked event counts as one message.

If limits are exceeded in Lens Studio, an error will print in the Logger and the session may disconnect.
