---
sidebar_position: 7
sidebar_label: CLI
description: 'Install and use the Snap Cloud CLI to create projects, deploy functions, manage databases, and monitor your backend from the terminal.'
---

import ThemedImage from '@theme/ThemedImage';

<ThemedImage
  alt="Supabase Logo"
  sources={{
    light: '/img/spectacles/supabase/logo-small-light.svg',
    dark: '/img/spectacles/supabase/logo-small-dark.svg',
  }}
  style={{
    maxWidth: '1000px',
    width: '20%',
    borderRadius: '5px',
    float: 'right',
  }}
/>

# CLI

The [Snap Cloud] CLI enables you to interface with your projects using a command line interface, and perform various tasks locally on different platforms. You can use it to manage databases and storage, deploy edge functions, run testing, and more without accessing the [Snap Cloud] Dashboard.

### Installing the CLI

To install the CLI:

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="macOS">

```ts

brew install supabase/tap/supabase

```

</TabItem>
  <TabItem value="Windows">

```
ts scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase

```

  </TabItem>
</Tabs>

### Updating the CLI

To update the CLI:

<Tabs>
<TabItem value="macOS">

```ts

brew upgrade supabase

```

</TabItem>
<TabItem value="Windows">

```ts

scoop update supabase

```

</TabItem>
</Tabs>

### Using the CLI

To use the CLI invoke `supabase` with the `snap` profile, followed by your command:

```ts

supabase --profile snap [command]

```

:::note

The CLI remembers your last used profile, so you do not need to specify `--profile snap` on each successive CLI command.

:::

For example:

```ts

supabase --profile snap login

```

For a full list of CLI commands, see the CLI [reference](https://cloud.snap.com/docs/reference/cli/introduction).

[Snap Cloud]: https://cloud.snap.com/
